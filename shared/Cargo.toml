[package]
name = "shared"
version = "0.1.0"
authors = ["rjman", "hacpy"]
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "^1.0"
serde_derive = "1.0"
bytes = "1.1.0"